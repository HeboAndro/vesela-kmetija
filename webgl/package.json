{
  "name": "kmetija-igra-webgl",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "mkdir -p public && cp -f ../public/mapa.png public/mapa.png && vite --host 0.0.0.0",
    "build": "mkdir -p public && cp -f ../public/mapa.png public/mapa.png && tsc && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5174"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "~5.6.3",
    "vite": "^5.4.11"
  }
}
